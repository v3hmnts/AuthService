databaseChangeLog:
  - changeSet:
      id: 001-Create db schema
      author: Vhmnts
      changes:
        - sql:
            sql: CREATE SCHEMA IF NOT EXISTS auth_db;
        - sql:
            sql: ALTER DATABASE auth_db SET search_path TO auth_db, public;